# Backend — Clinical Voice Agent (FastAPI + Python 3.12)
#
# Python 3.12 no es opcional: kokoro-onnx declara requires_python <3.14 y numba
# (dependencia del core de Pipecat) tampoco soporta 3.14.
#
# Ninguna dependencia arrastra PyTorch. Verificado en los metadatos de PyPI:
# pipecat-ai[kokoro] depende de kokoro-onnx, no del paquete `kokoro`.
#
# Las versiones están fijadas desde el entorno donde se midió y se probó todo
# (ver docs/spikes-7-agosto.md), no elegidas a mano.

fastapi==0.121.2
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.15.0
python-multipart==0.0.20

# Estado de la conversación: SQLite, sin servidor y sin Docker (ADR-014).
SQLAlchemy==2.0.51

# RAG: ChromaDB persistente + embeddings bge-m3 servidos por el mismo Ollama.
chromadb==1.5.9
pymupdf==1.28.2          # parseo de PDF conservando nº de página (RF-05)
PyYAML==6.0.3            # umbrales del motor de decisión

# LLM: llama3.2:3b vía Ollama — compuerta G3 (ADR-010).
# De los cuatro modelos permitidos, Gemini 1.5 Flash está retirado y Llama 3.1 70B
# fue decomisionado de Groq; ver docs/spikes-7-agosto.md.
ollama==0.6.2

# STT: Groq Whisper. G3 restringe el modelo que razona, no el de reconocimiento
# de voz, así que usarlo no compromete la compuerta.
groq==1.6.0

# Tests
pytest==9.1.1
httpx==0.28.1            # los tests comprueban si Ollama está vivo antes de correr
